node_modules/
test-results/
playwright-report/
blob-report/
.playwright/

# Raw per-page Playwright recordings — large, intermediate; the final composited
# demo-output/*.mp4 are what we keep.
demo-output/video-raw*/

# Raw docs-screencast recordings — regenerated by docs-screencasts.spec.ts and
# composited into odoo-addons/tvbo_platform_docs/static/video/*.mp4 by
# process-docs-videos.sh; only the processed mp4s are committed.
screencast-raw/
